export default class SetupConfiguration {
    constructor(port: String, message: String) {
        this.port = port;
        this.message = message;
    }
    public port: String;
    public message: String;
};